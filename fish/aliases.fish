alias bandkar "sudo shutdown now"
alias waapas "sudo reboot"
alias netbc " sudo usermod -G netdev -a kaustubh"
# add install error aliases

# git and parts
alias gst "git status"
alias ga "git add"
alias gc "git commit"
